# 当前 PR 状态

原始变更请求：调用方会使用 `idempotency_key` 重试任务提交；重复请求不应创建或派发第二个任务。

上一位智能体的实现：

- 在 `JobService` 中增加 `_idempotency` 字典；
- 首次提交时创建 `JobRecord`，写入存储，并调用派发器；
- 相同 key 再次提交时，从服务进程内的字典找到旧任务；
- 增加了三个公开的正常路径测试；
- 自报 `./verify.sh` 通过，认为可以合并。

尚未解决的审阅意见：

> `JobService` 的生命周期是否真的拥有幂等状态？如果服务被重新创建，当前实现依赖的事实还存在吗？

> 两个包装器同时看不到 key 时，会不会各自插入并派发？`insert` 和 key 预留的原子边界在哪里？

当前没有完整复现生产事故，也没有覆盖包装器重建、冲突 `payload`、并发准入或失败恢复的测试。派发器对“调用失败”的返回语义目前只覆盖确定拒绝；是否存在结果不可判定的失败，会在后续阶段补充。

仓库中已经存在 `MemoryJobBackend`，多个 `JobStore` 包装器可以复用同一个后端对象。现有公开测试每次只创建一个存储和一个服务，因此没有覆盖共享后端、包装器重建或并发调用。
